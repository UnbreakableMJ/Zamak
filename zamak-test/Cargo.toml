# SPDX-License-Identifier: GPL-3.0-or-later

[package]
name = "zamak-test"
version = "0.7.0"
edition = "2021"
description = "QEMU integration test harness for the ZAMAK bootloader"
license = "GPL-3.0-or-later"

[[bin]]
name = "zamak-test"
path = "src/main.rs"

[dependencies]
